import React, { useState, useEffect } from "react";
import { Button } from "antd";

export interface SideTabBarProps {}

const SideTabBar: React.FC<SideTabBarProps> = (props) => {
  const {} = props;

  return <Button>+1</Button>;
};

export default SideTabBar;
